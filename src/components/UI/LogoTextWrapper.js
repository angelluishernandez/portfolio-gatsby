import styled from "styled-components"

export const LogoTextWrapper = styled.h6`
  padding: 5px 0;
  border: 1px dashed var(--background);
  color: var(--background) !important;
  text-align: center;
`
